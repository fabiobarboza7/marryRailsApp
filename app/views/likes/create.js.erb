function refreshForm(innerHTML) {
  const reRenderLike = document.getElementById('render-profiles');
  reRenderLike.innerHTML = innerHTML;
}

<% if @like.errors.any? %>
  alert("error");
<% else %>
  refreshForm('<%= j render "profiles/form" %>');
<% end %>